precommit: |
  # parallel
  # dep: test
  # dep: black

test: |
  # watch: pfiles
  # arg: py (unittest args)
  python tests/test.py $PY

pfiles: |
  find -type f -name "*.py"

black: |
  # watch: pfiles
  black .

cfail: |
  echo wow

fail: |
  # watch: 1
  # catch: cfail
  echo running
  exit 1

long: |
  # watch: echo test.txt
  echo hi
  sleep 5
  echo done

watch: |
  # watch: echo test.txt
  echo start
  sleep 3
  echo done
